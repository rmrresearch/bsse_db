[project]
name = "bsse_db"
version = "0.0.1"
description = "Database of BSSE calculations used to understand VMFC"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [
    { name = "Ibrahim Sufi"}
]
dependencies = [
    "pytest",
    "rdkit",
    "py3Dmol",
    "jupyterlab",
    "pandas"
]
